- if can? :view, :leagues_completed
  .row
    .col-sm-12
      .page-header
        / page title
        %h1.pull-left
          %i.icon-sort-by-order
          %span= I18n.t("site.visited_clubs.title")
        / breadcrumbs
        .pull-right
          %ul.breadcrumb
            %li
              = link_to root_path do
                %i.icon-dashboard
            %li.separator
              %i.icon-sort-by-order
            %li.active= I18n.t("site.visited_clubs.title")

